#!/usr/bin/env bash
# display fizz, buzz or fizzbuzz
count=1
while [ $count -ne 101 ]
do
	if [ $((count % 3)) -eq 0 ]
	then
		echo "Fizz"
	elif [ $((count % 5)) -eq 0 ]
	then
		echo "Buzz"
	elif [ $((count % 15)) -eq 0 ]
	then
		echo "FizzBuzz"
	else
		echo $count
	fi
	((count++))
done
